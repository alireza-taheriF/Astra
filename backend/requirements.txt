# Astra backend — capability engine dependencies
# Web framework (the engine runs inside FastAPI async request handlers).
fastapi>=0.110
# Async HTTP client for the GitHub REST/Git-data API + FastAPI TestClient.
httpx>=0.27
# Supabase service-role client for the backend-only badge read.
supabase>=2.4

# Deterministic static analysis. Python uses the stdlib `ast`; the following
# grammars drive TreeSitterParser for C/C++/CUDA, JavaScript, and TypeScript.
tree-sitter>=0.23,<0.27
tree-sitter-cpp>=0.23
tree-sitter-javascript>=0.23
tree-sitter-typescript>=0.23

# Testing.
pytest>=8.0
pytest-asyncio>=0.23
uvicorn>=0.20.0